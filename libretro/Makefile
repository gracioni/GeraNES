TARGET_NAME := geranes
CORE_SUFFIX := _libretro

CXX ?= g++
CC ?= gcc
AR ?= ar
PYTHON ?= $(shell command -v python 2>/dev/null || command -v python3 2>/dev/null)

SRC_DIR := ../src
CORE_DIR := $(SRC_DIR)/libretro
DB_FILE := ../data/db.txt
EMBEDDED_DB_SRC := $(CORE_DIR)/generated/EmbeddedDbData.cpp
EMBEDDED_DB_GEN := ./gen_embedded_db.py

SOURCES_CXX := \
	$(CORE_DIR)/GeraNESLibretro.cpp \
	$(EMBEDDED_DB_SRC) \
	$(SRC_DIR)/signal/signal.cpp

SOURCES_C := \
	$(SRC_DIR)/zip/zip.c

OBJECTS := $(SOURCES_CXX:.cpp=.o) $(SOURCES_C:.c=.o)

INCLUDES := -I.. -I$(SRC_DIR)
DEFINES := -DGERANES_DISABLE_PATCHES

CXXFLAGS += -std=gnu++20 -fPIC $(INCLUDES) $(DEFINES)
CFLAGS += -std=gnu11 -fPIC $(INCLUDES) $(DEFINES)
LDFLAGS +=
LIBS +=

EXT := so
SHARED := -shared

ifeq ($(platform), osx)
	EXT := dylib
	SHARED := -dynamiclib
	LDFLAGS += -Wl,-dead_strip
endif

ifneq (,$(findstring ios,$(platform)))
	EXT := dylib
	SHARED := -dynamiclib
endif

ifneq (,$(findstring win,$(platform)))
	EXT := dll
	SHARED := -shared
	LDFLAGS += -Wl,--no-undefined
endif

ifneq (,$(findstring mingw,$(platform)))
	EXT := dll
	SHARED := -shared
	LDFLAGS += -Wl,--no-undefined
endif

ifeq ($(platform), emscripten)
	EXT := bc
	SHARED :=
endif

TARGET := $(TARGET_NAME)$(CORE_SUFFIX).$(EXT)

all: $(TARGET)

$(TARGET): $(OBJECTS)
	$(CXX) $(SHARED) -o $@ $^ $(LDFLAGS) $(LIBS)

$(EMBEDDED_DB_SRC): $(DB_FILE) $(EMBEDDED_DB_GEN)
	@mkdir -p $(dir $@)
	@if command -v xxd >/dev/null 2>&1; then \
		echo "// Auto-generated by libretro/Makefile. Do not edit." > $(EMBEDDED_DB_SRC); \
		xxd -i -n geranes_libretro_embedded_db $(DB_FILE) >> $(EMBEDDED_DB_SRC); \
		echo "const unsigned int geranes_libretro_embedded_db_size = geranes_libretro_embedded_db_len;" >> $(EMBEDDED_DB_SRC); \
	elif [ -n "$(PYTHON)" ]; then \
		$(PYTHON) $(EMBEDDED_DB_GEN) $(DB_FILE) $(EMBEDDED_DB_SRC); \
	else \
		echo "Need xxd or Python (python/python3) to embed db.txt"; \
		false; \
	fi

%.o: %.cpp
	$(CXX) $(CXXFLAGS) -c $< -o $@

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f $(OBJECTS) $(TARGET) $(EMBEDDED_DB_SRC)

.PHONY: all clean
